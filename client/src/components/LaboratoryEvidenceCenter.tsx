import { CheckCircle2, ChevronRight, ClipboardCheck, FileSearch, FlaskConical, Link2, ShieldQuestion } from "lucide-react";
import { Link } from "wouter";
import {
  EVIDENCE_LIFECYCLE,
  EVIDENCE_RECORD_FIELD_DEFINITIONS,
  getEvidenceCompleteness,
  getEvidenceLifecycleStatus,
  LABORATORY_EVIDENCE_RECORDS,
  type EvidenceCompleteness,
  type EvidenceFieldStatus,
  type EvidenceScientificClassification,
} from "@/lib/laboratoryEvidence";

function statusClass(status: EvidenceFieldStatus | EvidenceCompleteness | EvidenceScientificClassification) {
  if (status === "SIMULATION") return "border-violet-500/35 bg-violet-500/10 text-violet-200";
  if (status === "DERIVED") return "border-amber-500/35 bg-amber-500/10 text-amber-200";
  if (status === "MEASURED" || status === "VERIFIED") return "border-emerald-500/35 bg-emerald-500/10 text-emerald-200";
  if (status === "PARTIAL" || status === "COMPLETE") return "border-amber-500/35 bg-amber-500/10 text-amber-200";
  return "border-slate-700 bg-slate-900/70 text-slate-400";
}

function StatusChip({ status }: { status: EvidenceFieldStatus | EvidenceCompleteness | EvidenceScientificClassification }) {
  return <span className={`inline-flex rounded border px-1.5 py-0.5 font-mono text-[7px] tracking-[0.11em] ${statusClass(status)}`}>{status}</span>;
}

const evidenceFlow = ["SAMPLE", "MEASUREMENT", "INSTRUMENT", "CALIBRATION", "TRACEABILITY", "UNCERTAINTY", "EVIDENCE", "VERIFICATION"] as const;

export function LaboratoryEvidenceCenter() {
  const records = LABORATORY_EVIDENCE_RECORDS;
  const completeness = getEvidenceCompleteness(records);
  return <section id="laboratory-evidence-center" className="border border-emerald-500/25 bg-slate-950/70 p-4 shadow-[0_0_50px_rgba(16,185,129,0.08)]"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex items-center gap-2 text-[9px] tracking-[0.22em] text-emerald-300"><FlaskConical className="h-3.5 w-3.5" />LABORATORY EVIDENCE & DATA INGESTION FOUNDATION</div><h2 className="mt-1 text-lg font-semibold text-slate-100">LABORATORY EVIDENCE CENTER</h2><p className="mt-1 max-w-3xl text-[10px] leading-relaxed text-slate-500">A UI and contract foundation for future real laboratory evidence. No evidence record, laboratory value, sample, certificate, calibration result, uncertainty value, or verification claim is loaded in this Control Room.</p></div><div className="flex gap-1.5"><StatusChip status={completeness} /><StatusChip status="NOT LOADED" /></div></div><div className="mt-3 flex flex-wrap gap-1.5 text-[8px]"><a href="#instrument-registry" className="border border-cyan-500/30 px-2 py-1 text-cyan-200 hover:bg-cyan-500/10">INSTRUMENT REGISTRY</a><a href="#metrological-traceability" className="border border-sky-500/30 px-2 py-1 text-sky-200 hover:bg-sky-500/10">TRACEABILITY CHAIN</a><a href="#uncertainty-budget" className="border border-violet-500/30 px-2 py-1 text-violet-200 hover:bg-violet-500/10">UNCERTAINTY BUDGET</a><Link href="/knowledge"><span className="inline-flex cursor-pointer border border-emerald-500/30 px-2 py-1 text-emerald-200 hover:bg-emerald-500/10">KNOWLEDGE CENTER</span></Link></div><section className="mt-4 border border-emerald-500/20 bg-slate-950/85 p-3"><div className="flex flex-wrap items-center justify-between gap-3"><div><div className="flex items-center gap-2 text-[8px] tracking-[0.16em] text-emerald-300"><Link2 className="h-3.5 w-3.5" />EVIDENCE FLOW</div><p className="mt-1 text-[9px] text-slate-500">Relationship contract only; no sample or measurement is instantiated.</p></div><span className="font-mono text-[8px] text-slate-500">RECORDS LOADED: {records.length}</span></div><div className="mt-3 flex flex-wrap items-center gap-1.5">{evidenceFlow.map((step, index) => <div key={step} className="flex items-center gap-1.5"><span className={`rounded border px-2 py-1 font-mono text-[8px] ${step === "VERIFICATION" ? "border-slate-700 bg-slate-900/70 text-slate-400" : "border-emerald-500/25 bg-emerald-500/5 text-emerald-200"}`}>{step}{step === "VERIFICATION" ? " / NOT LOADED" : ""}</span>{index < evidenceFlow.length - 1 ? <ChevronRight className="h-3.5 w-3.5 text-emerald-400" /> : null}</div>)}</div></section><div className="mt-3 grid gap-3 xl:grid-cols-[1fr_.85fr]"><section className="border border-slate-800 bg-slate-950/80 p-3"><div className="flex items-center gap-2 text-[8px] tracking-[0.16em] text-cyan-300"><ClipboardCheck className="h-3.5 w-3.5" />EVIDENCE RECORD CONTRACT</div><div className="mt-3 grid gap-2 sm:grid-cols-2 xl:grid-cols-3">{EVIDENCE_RECORD_FIELD_DEFINITIONS.map(field => <article key={field.key} className="border border-slate-800 bg-slate-950/90 p-2"><div className="flex items-center justify-between gap-2"><span className="text-[8px] font-semibold tracking-[0.1em] text-slate-200">{field.label}</span><StatusChip status="NOT LOADED" /></div><p className="mt-1 text-[8px] leading-relaxed text-amber-100">LIMIT: {field.interpretationLimit}</p></article>)}</div></section><section className="border border-slate-800 bg-slate-950/80 p-3"><div className="flex items-center gap-2 text-[8px] tracking-[0.16em] text-sky-300"><FileSearch className="h-3.5 w-3.5" />EVIDENCE INSPECTOR</div><div className="mt-3 rounded border border-slate-800 bg-slate-950/90 p-3"><div className="flex items-center justify-between gap-3"><div className="text-[10px] font-semibold text-slate-200">NO EVIDENCE RECORD LOADED</div><StatusChip status="NOT LOADED" /></div><div className="mt-3 space-y-2 text-[9px] leading-relaxed"><p><span className="text-slate-600">AVAILABLE:</span> Evidence Record contract, lifecycle, completeness model, contextual links.</p><p><span className="text-slate-600">MISSING:</span> evidence ID, sample, measurement, source, certificate, calibration, laboratory, standard, uncertainty, dataset, and verification evidence.</p><p><span className="text-slate-600">SOURCE:</span> NOT LOADED</p><p><span className="text-slate-600">PROVENANCE:</span> UNKNOWN</p><p><span className="text-slate-600">TRACEABILITY:</span> NOT LOADED</p><p><span className="text-slate-600">CALIBRATION:</span> NOT LOADED</p><p><span className="text-slate-600">UNCERTAINTY:</span> NOT LOADED</p><p><span className="text-slate-600">VERIFICATION:</span> NOT LOADED</p><p className="border-t border-slate-800 pt-2 text-amber-100"><span className="text-amber-300/70">INTERPRETATION LIMIT:</span> This empty state is not laboratory evidence, a measurement result, or a verification decision.</p></div></div></section></div><section className="mt-3 border border-slate-800 bg-slate-950/80 p-3"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex items-center gap-2 text-[8px] tracking-[0.16em] text-violet-300"><CheckCircle2 className="h-3.5 w-3.5" />EVIDENCE LIFECYCLE & COMPLETENESS</div><p className="mt-1 text-[9px] text-slate-500">Lifecycle status is shown as contract availability only. No lifecycle starts until real evidence is received.</p></div><div className="flex gap-1.5"><StatusChip status={completeness} /><span className="rounded border border-amber-500/30 bg-amber-500/10 px-1.5 py-0.5 font-mono text-[7px] tracking-[0.11em] text-amber-200">COMPLETE ≠ VERIFIED</span></div></div><div className="mt-3 flex flex-wrap gap-1.5">{EVIDENCE_LIFECYCLE.map(stage => <span key={stage} className="rounded border border-slate-800 bg-slate-900/70 px-2 py-1 font-mono text-[7px] tracking-[0.08em] text-slate-400">{stage} / {getEvidenceLifecycleStatus(records, stage)}</span>)}</div></section><div className="mt-3 rounded border border-amber-500/20 bg-amber-500/5 p-2 text-[8px] leading-relaxed text-amber-100"><ShieldQuestion className="mr-1 inline h-3 w-3 text-amber-300" />Scientific classification: <span className="text-violet-200">SIMULATION</span> remains a digital-twin source; <span className="text-amber-200">DERIVED</span> remains inferred presentation; <span className="text-emerald-200">MEASURED</span> requires real loaded evidence; <span className="text-slate-300">UNKNOWN</span> remains unknown. SIMULATION ≠ MEASURED. DERIVED ≠ MEASURED. UNKNOWN ≠ ZERO.</div></section>;
}
